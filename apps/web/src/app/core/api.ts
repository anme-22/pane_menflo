/**
 * Base de la API. Relativa a propósito: en desarrollo el dev-server la
 * redirige con el proxy (`proxy.conf.json`) y en producción la app se sirve
 * detrás del mismo origen que la API. Nada de hosts hardcodeados.
 */
export const API_BASE = '/api';
